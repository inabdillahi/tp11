<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link rel="stylesheet" href="https://bootswatch.com/5/zephyr/bootstrap.min.css">
    <link rel="stylesheet" href="{{asset('assets/css/style.css')}}">
    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
    {% endblock %}
</head>

<body>

    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
        <div class="container-fluid">
            <a class="navbar-brand btn btn-md mx-2  btn-success" href="{{path('home')}}">TP11:article</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{path('home')}}"><i class="fa-solid fa-house"></i> Accueil</a>
                    </li>

                    {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{path('mes_annonces')}}"><i class="fa-solid fa-list"></i> Mes
                            annonces</a>
                    </li>
                    {% endif %}


                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin')}}"><i class="fa-solid fa-screwdriver-wrench"></i>
                            Admin</a>
                    </li>
                    {% endif %}

                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('profile')}}"><i class="fa-solid fa-address-card"></i>
                            Profil</a>
                    </li>
                    {% endif %}


                </ul>
                <div class="d-flex">
                    {% if app.user %}
                    <a href="{{path('logout')}}" class="btn btn-sm mx-2  btn-danger"><i
                            class="fa-solid fa-right-from-bracket"></i> Logout</a>
                    {% else %}
                    <a href="{{path('login')}}" class="btn btn-sm  mx-2 btn-success"><i
                            class="fa-solid fa-right-to-bracket"></i> Login</a>
                    <a href="{{path('register')}}" class="btn btn-sm mx-2  btn-warning"><i
                            class="fa-regular fa-id-card"></i> Register</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>
    <div class="container">
        {% block body %}{% endblock %}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/0c4c2b9540.js" crossorigin="anonymous"></script>
</body>

</html>